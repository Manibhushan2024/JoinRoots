[providers]
  php = "8.2"
  node = "18"

[phases.setup]
  nixPkgs = ["php82", "php82Packages.composer", "php82Extensions.pdo_pgsql", "php82Extensions.pgsql", "nodejs_18", "npm"]

[phases.install]
  cmds = ["composer install --no-dev --optimize-autoloader", "npm install"]

[phases.build]
  cmds = ["npm run build", "php artisan config:cache", "php artisan route:cache", "php artisan view:cache"]

[start]
  cmd = "php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"
